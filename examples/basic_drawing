# Basic drawing program for pchip programming language
# Draws the number 1 on the screen, moving it to the right when a key is pressed
{
    let x = 0;
    loop {
        draw_pos(x, 5);
        draw5(get_font(1));
        key_wait();
        clear();
        x = plus(x, 1);
    }
}
